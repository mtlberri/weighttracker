{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      },
      "default_user": {
        ".read": true,
        ".write": false        
      }
    }
  }
}